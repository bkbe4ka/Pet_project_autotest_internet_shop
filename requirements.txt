selenium>=4.0
pytest>=7.0
allure-pytest>=2.13
# selfheal встроен в проект (папка selfheal/), отдельная установка не нужна.
# Опционально для более сильного семантического матча:
# sentence-transformers>=2.2
